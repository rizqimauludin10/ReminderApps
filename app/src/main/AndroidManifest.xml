<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.reminderapps">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.DetailProfilActivity"></activity>
        <activity android:name=".activity.ProfilActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<service
            android:name=".ServicesReminder"
            android:enabled="true"
            android:exported="true" />-->

        <receiver
            android:name=".utils.BroadcastReminder"
            android:enabled="true"
            android:exported="true" />
        <receiver
            android:name=".utils.BroadcastReminder2"
            android:enabled="true"
            android:exported="true" />
        <receiver
            android:name=".utils.BroadcastReminder3"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Main3Activity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".activity.MainActivity">

        </activity>
    </application>

</manifest>